# 安裝/更新指南

## 🔄 更新現有版本

如果您已經安裝了舊版本,在安裝新版本時可能會遇到「無法關閉」的提示。

### 問題: "geniideas flowchart 無法關閉,請手動關閉它"

這是因為舊版本的應用還在運行中。請按照以下步驟解決:

---

## ✅ 解決方案

### **方法 1: 使用工作管理員關閉** (推薦)

#### 步驟 1: 打開工作管理員
- 按 `Ctrl + Shift + Esc`
- 或右鍵點擊工作列 → 選擇「工作管理員」

#### 步驟 2: 找到並關閉應用
1. 在「處理程序」標籤中尋找:
   - `Geniideas Flowchart`
   - 或任何包含 "geniideas" 的程序

2. 右鍵點擊該程序
3. 選擇「結束工作」

#### 步驟 3: 確認完全關閉
切換到「詳細資料」標籤,確認沒有:
- `Geniideas Flowchart.exe`
- `electron.exe` (相關的)

#### 步驟 4: 繼續安裝
返回安裝程式,點擊「重試」按鈕

---

### **方法 2: 使用命令行強制關閉**

#### 步驟 1: 打開命令提示字元
- 按 `Win + R`
- 輸入 `cmd`
- 按 Enter

#### 步驟 2: 執行關閉命令
複製並執行以下命令:

```cmd
taskkill /F /IM "Geniideas Flowchart.exe"
```

如果看到「找不到該處理程序」,試試:
```cmd
taskkill /F /IM electron.exe
```

#### 步驟 3: 繼續安裝
返回安裝程式,點擊「重試」

---

### **方法 3: 完全解除安裝後重新安裝**

如果上述方法都無效:

#### 步驟 1: 解除安裝舊版本
1. 按 `Win + I` 打開設定
2. 應用程式 → 已安裝的應用程式
3. 搜尋「Geniideas Flowchart」
4. 點擊「...」→ 解除安裝
5. 按照提示完成解除安裝

#### 步驟 2: 確認程序已關閉
- 打開工作管理員
- 確認沒有相關程序在運行

#### 步驟 3: 清理殘留文件 (可選)

**如果要保留數據** (專案、設定等):
- 跳過此步驟,直接進行步驟 4

**如果要完全清理**:
1. 按 `Win + R`
2. 輸入: `%APPDATA%`
3. 找到並刪除 `geniideas-vue` 資料夾

**備份數據** (建議):
在刪除前,先備份以下文件:
```
C:\Users\[您的用戶名]\AppData\Roaming\geniideas-vue\db\
├── flowcharts.db          # 您的所有專案數據
└── backups/               # 自動備份
```

#### 步驟 4: 安裝新版本
執行新的安裝程式 `Geniideas Flowchart Setup 1.0.0.exe`

---

### **方法 4: 重啟電腦** (最保險)

如果以上方法都無效:

1. **儲存所有工作**
2. **重啟 Windows 電腦**
3. **重啟後立即執行安裝程式**

這樣可以確保沒有任何程序在運行。

---

## 🆕 全新安裝

如果這是第一次安裝:

### 步驟 1: 下載安裝程式
獲取 `Geniideas Flowchart Setup 1.0.0.exe`

### 步驟 2: 執行安裝
1. 雙擊安裝程式
2. 選擇安裝位置 (建議使用默認位置)
3. 選擇是否創建桌面捷徑
4. 點擊「安裝」

### 步驟 3: 完成安裝
- 安裝完成後,可以選擇立即啟動
- 或從桌面/開始菜單啟動

---

## 🔒 新功能: 單一實例鎖定

**從新版本開始**,應用已加入單一實例鎖定功能:

### 功能說明
- ✅ 應用只能同時運行一個實例
- ✅ 如果嘗試啟動第二個實例,會自動聚焦到現有窗口
- ✅ 防止多個實例衝突

### 好處
- 避免數據庫衝突
- 避免端口占用問題
- 更新時更容易管理

---

## 📁 安裝位置

### 默認安裝路徑
```
C:\Program Files\Geniideas Flowchart\
```

### 數據存儲位置
```
C:\Users\[您的用戶名]\AppData\Roaming\geniideas-vue\
├── db\                    # 數據庫
│   ├── flowcharts.db     # 主數據庫
│   └── backups\          # 自動備份
└── logs\                  # 日誌文件
```

---

## ⚙️ 系統需求

### 最低需求
- **作業系統**: Windows 10 (64-bit) 或更高
- **記憶體**: 4GB RAM
- **硬碟空間**: 500MB 可用空間
- **處理器**: Intel Core i3 或同等級

### 建議需求
- **作業系統**: Windows 11 (64-bit)
- **記憶體**: 8GB RAM
- **硬碟空間**: 1GB 可用空間
- **處理器**: Intel Core i5 或更高

---

## 🔧 安裝後設定

### 首次啟動
1. 應用會自動創建數據庫
2. 數據存儲在用戶目錄
3. 不需要額外設定

### 可選設定
- **PDF 匯出**: 安裝 LibreOffice (https://www.libreoffice.org/)
- **防火牆**: 允許應用通過防火牆 (如果被提示)

---

## ❓ 常見問題

### Q1: 安裝時需要管理員權限嗎?
**A**: 如果安裝到 `Program Files`,需要管理員權限。可以選擇安裝到用戶目錄避免此問題。

### Q2: 可以自訂安裝位置嗎?
**A**: 可以。安裝時選擇「自訂安裝」,然後選擇您想要的位置。

### Q3: 更新會保留我的數據嗎?
**A**: 會。數據存儲在用戶目錄 (`%APPDATA%`),不會被更新影響。

### Q4: 可以同時安裝多個版本嗎?
**A**: 不建議。新版本會覆蓋舊版本,且應用有單一實例鎖定。

### Q5: 如何完全移除應用?
**A**: 
1. 解除安裝應用
2. 刪除 `%APPDATA%\geniideas-vue` 資料夾
3. (可選) 刪除安裝目錄

---

## 🆘 需要幫助?

如果遇到安裝問題:

1. **查看日誌**: `%APPDATA%\geniideas-vue\logs\`
2. **參考故障排除**: 查看 `TROUBLESHOOTING.md`
3. **檢查系統需求**: 確認符合最低需求
4. **防毒軟體**: 暫時停用防毒軟體試試

---

## 📝 檢查清單

安裝前:
- [ ] 確認系統符合需求
- [ ] 關閉舊版本應用
- [ ] (更新時) 備份重要數據

安裝中:
- [ ] 選擇安裝位置
- [ ] 選擇是否創建捷徑
- [ ] 等待安裝完成

安裝後:
- [ ] 啟動應用測試
- [ ] 檢查數據是否保留 (更新時)
- [ ] 測試基本功能

---

**版本**: 1.0.0  
**更新日期**: 2025-11-24  
**狀態**: ✅ 可用
